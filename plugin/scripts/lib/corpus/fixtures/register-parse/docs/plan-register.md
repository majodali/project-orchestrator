# Plan register (fixture)

Provokes register-parse: a node-like line (starts with a node ID)
that fails the grammar because its stage bracket never closes.

- P1-N001 [done] Fine node
  - P1-N002 [executing missing-close-bracket bad line
  - P1-N003 [done] Also fine
